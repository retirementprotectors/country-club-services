# crm/config.js IS committed — contains browser-restricted Maps key (referrer-locked).
# Server-side keys (.geocode-cache.json, etc.) remain ignored below.
# Geocoding cache — raw API responses, not committed (regenerate with geocode-neighborhood.js)
data/.geocode-cache.json

# Maps API key — never commit a real key

# OS / editor
.DS_Store
Thumbs.db
.vscode/
*.swp
data/.streetview-cache.json
